# @aas/recorder-null

Recorder plugin that records nothing. For developing and testing games and runtimes only; a run made with it is never a valid AI Assisted Speedrun (the spec requires a recording). `aas run` uses it when no `--recorder` is given and warns.
